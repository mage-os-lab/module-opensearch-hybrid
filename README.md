# module-opensearch-hybrid
OpenSearch Hybrid is an open-source module for Mage-OS 3.5 that combines keyword relevance with semantic reranking of the top 100 search results. It preserves native facets and result counts, supports per-store activation, and falls back to native search when needed. Currently an engineering pilot.
